# Backend API (GitHub OAuth client id/secret live on the server — use /auth/login from the app)
VITE_API_BASE_URL=http://localhost:8000

# Optional: GitHub App slug for install links in the UI
VITE_GITHUB_APP_SLUG=your-github-app-slug
